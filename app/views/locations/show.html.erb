<div class="container">
  <nav class="navbar navbar-inverse">
    <p class="navbar-title"><%= @location.name %></p>
  </nav>
</div><br>

<div class="container">
  <div class="well">
    <div class="row">
      <div class="col-lg-12">
        <p>
          <strong>Phone:</strong>
          <%= @location.phone %>
        </p>
        <p>
          <strong>Address:</strong>
          <%= @location.address %>
        </p>
        
        <%= render 'employees/employee_list' %>
        <%= link_to 'Edit', edit_location_path(@location) %> |
        <%= link_to 'Back', locations_path %>
      </div>
    </div>
  </div>
</div>
