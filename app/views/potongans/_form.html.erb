<%= form_for(@potongan) do |f| %>
  <% if @potongan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@potongan.errors.count, "error") %> prohibited this potongan from being saved:</h2>

      <ul>
      <% @potongan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nama %><br>
    <%= f.text_field :nama %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
